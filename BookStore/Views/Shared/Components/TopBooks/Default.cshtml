@model IEnumerable<BookViewModel>
<div class="row">
    @if (Model != null)
    {
        @foreach (var book in Model)

        {
            <div class="col-md-4">

                <partial name="_bookThumbnail" model="book" />
            </div>

        }
    }
</div>